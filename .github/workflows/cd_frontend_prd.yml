name: CD - frontend (prd)

on:
  push:
    tags:
      - "frontend/v*"

jobs:
  cd:
    uses: ./.github/workflows/_cd_frontend.yml
    with:
      environment: prd
    secrets:
      vercel-token: ${{ secrets.VERCEL_TOKEN }}
